#! /usr/bin/env php
<?php

require __DIR__ . "/../vendor/autoload.php";

use function Games\generatePrimeRound;

runGame(
    'Answer "yes" if given number is prime. Otherwise answer "no".',
    generatePrimeRound()
);
